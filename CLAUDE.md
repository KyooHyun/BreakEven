# CLAUDE.md — 작업 규칙

이 프로젝트는 카드사 데이터 분석 직무 지원용 개인 프로젝트다.
상세 기획은 `PROJECT_SPEC.md` 참조.

## 절대 규칙

### 1. 수치를 지어내지 않는다
- 모든 수치는 **실제 스크립트 실행 결과**에서만 나온다.
- 코드를 돌리지 않은 상태에서 "20대는 배달 소비가 높은 경향" 같은
  그럴듯한 해석 문장을 절대 쓰지 않는다.
- 아직 검증되지 않은 내용은 `[미검증]` 태그를 붙여 명시한다.

### 2. 모든 해석에는 근거를 첨부한다
- 문서에 해석 문장을 쓸 때는 반드시 근거가 되는
  출력 테이블 경로 또는 스크립트명을 함께 적는다.
- 예: `(근거: outputs/tables/segment_profile.csv, src/03_segment.py)`

### 3. 가정은 숨기지 않고 드러낸다
- 수수료율 등 외부 가정치는 `config/assumptions.yaml`에 분리해 관리한다.
- 코드 안에 하드코딩하지 않는다.
- 가정치를 쓴 모든 결론은 민감도 분석 대상이다.

### 4. 최종 결론 문장은 사용자가 직접 쓴다
- README의 "결론" 섹션과 hypothesis-log의 함의 부분은
  자리만 잡아두고 사용자가 채우도록 남긴다.
- 이유: 자소서·면접에서 본인 언어로 설명할 수 있어야 한다.

### 5. 재현 가능해야 한다
- 각 단계는 독립 실행 가능한 스크립트로 분리한다.
- 중간 산출물은 `data/processed/`에 저장하고, 매번 재계산하지 않는다.
- 랜덤 시드를 고정한다.

## 코드 규칙

- Python, pandas 기반. 과도한 추상화 금지.
- 스크립트는 번호 접두사로 실행 순서를 표현한다.
  `src/01_load.py`, `02_clean.py`, `03_segment.py`, `04_economics.py`, `05_report.py`
- 각 스크립트 상단에 **입력 / 출력 / 목적**을 주석으로 명시한다.
- 머신러닝은 최소한으로. 이 프로젝트의 가치는 모델 성능이 아니라
  경제성 역산과 의사결정 설계에 있다.

## 데이터 검증 체크리스트 (L1에서 반드시 수행)

- [ ] 결측치 위치와 비율
- [ ] 매출금액 / 매출건수 = 건당 평균 결제액이 상식적 범위인가
- [ ] 특정 업종·시간대에 0 또는 극단값이 몰려 있지 않은가
- [ ] 데이터 기준 시점이 특수 기간(팬데믹 등)에 걸치는가
- [ ] 코드값이 규격서와 전부 매칭되는가 (미매칭 코드 목록화)

## 작업 로그

`docs/ai-workflow.md`에 아래를 기록한다. (자소서 소재로 사용)

- 어떤 지시가 잘 작동했고 어떤 지시가 빗나갔는가
- 사람이 판단해야 했던 지점은 어디였는가
- 결과물을 신뢰하기 위해 어떤 검증 절차를 넣었는가

## 하지 말 것

- 대시보드, 웹 서비스, API 서버 구축 (범위 밖)
- 딥러닝 모델 도입
- 결론을 먼저 정해두고 데이터를 맞추는 서술
- 데이터 한계를 얼버무리는 표현
